class Solution:
    def numJewelsInStones(self, jewels: str, stones: str) -> int:
        hashtable = {}
        count = 0
        for ch in jewels:
            hashtable[ch] = hashtable.get(ch, 0) +1
        for ch in stones:
            if ch in hashtable:
                count += 1
        return count 

        
