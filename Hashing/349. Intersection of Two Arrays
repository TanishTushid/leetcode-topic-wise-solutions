class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:

        s1 = set(nums1)
        s2 = set(nums2)
        store = []
        for num in s1:
            if num in s2:
                store.append(num)
        return store
